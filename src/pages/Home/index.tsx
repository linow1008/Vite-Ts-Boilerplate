import { Alert, AlertDescription, AlertTitle } from '@/components/ui/alert';

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <div>Test</div>
      <div className="h-screen w-full space-y-3 bg-slate-100 p-3">
        <div>h</div>
      </div>
    </div>
  );
};

export default Home;
